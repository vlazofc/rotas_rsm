diff --git a/apps/web/src/styles.css b/apps/web/src/styles.css
index 944dca0..268d948 100644
--- a/apps/web/src/styles.css
+++ b/apps/web/src/styles.css
@@ -9,8 +9,34 @@
   --soft: #f3f6f8;
   --danger: #d64545;
   --shadow: 0 8px 24px rgba(16, 24, 40, .08);
+  --shell-bg: #eef3f4;
+  --rail-bg: #f8fbfb;
 }
 
+[data-theme="dark"] {
+  --ink: #e5e9f0;
+  --muted: #94a3b8;
+  --line: #2a3548;
+  --panel: #1b2430;
+  --soft: #11161f;
+  --shadow: 0 8px 24px rgba(0, 0, 0, .4);
+  --shell-bg: #0d1117;
+  --rail-bg: #151c27;
+}
+[data-theme="dark"] body { background: var(--soft); }
+[data-theme="dark"] .app-shell { background: var(--shell-bg); }
+[data-theme="dark"] .icon-rail { background: var(--rail-bg); }
+[data-theme="dark"] .rail-dot { color: var(--ink); }
+[data-theme="dark"] .nav-link { color: var(--ink); }
+[data-theme="dark"] .data-table tbody tr:hover { background: rgba(255,255,255,.03); }
+[data-theme="dark"] .empty-state { color: var(--muted); }
+[data-theme="dark"] input, [data-theme="dark"] select, [data-theme="dark"] textarea {
+  background: var(--panel);
+  color: var(--ink);
+  border-color: var(--line);
+}
+[data-theme="dark"] .modal-backdrop { background: rgba(0,0,0,.6); }
+
 * { box-sizing: border-box; }
 html, body, #root { min-height: 100%; }
 body {
@@ -23,13 +49,161 @@ button, input, select, textarea { font: inherit; }
 button { touch-action: manipulation; }
 label { font-size: 13px; color: #334155; }
 
+.login-page {
+  min-height: 100vh;
+  min-height: 100dvh;
+  display: grid;
+  place-items: center;
+  padding: clamp(18px, 4vh, 42px) 16px;
+  background: #f1f5f9;
+  background-repeat: no-repeat;
+  background-size: cover;
+  background-position: center center;
+  overflow: hidden;
+}
+.login-page.has-background::before {
+  content: "";
+  position: fixed;
+  inset: 0;
+  background:
+    radial-gradient(circle at 50% 45%, rgba(15, 23, 42, .04), rgba(15, 23, 42, .18)),
+    linear-gradient(180deg, rgba(0, 0, 0, .04), rgba(0, 0, 0, .12));
+  pointer-events: none;
+}
+.login-card {
+  position: relative;
+  z-index: 1;
+  width: min(430px, calc(100vw - 32px));
+  padding: clamp(24px, 4vw, 34px);
+  border: 1px solid rgba(255, 255, 255, .58);
+  border-radius: 16px;
+  background: rgba(255, 255, 255, .78);
+  color: #111827;
+  box-shadow: 0 18px 48px rgba(15, 23, 42, .22);
+  backdrop-filter: blur(10px) saturate(1.1);
+}
+.login-brand {
+  display: flex;
+  justify-content: center;
+  align-items: center;
+  min-height: 76px;
+  margin-bottom: 10px;
+  text-align: center;
+}
+.login-brand h1 {
+  margin: 0;
+  font-size: 23px;
+  font-weight: 850;
+}
+.login-logo-frame {
+  display: inline-grid;
+  place-items: center;
+  min-width: 92px;
+  max-width: 220px;
+  min-height: 64px;
+  padding: 10px 14px;
+  border: 1px solid rgba(255, 255, 255, .62);
+  border-radius: 14px;
+  background: rgba(255, 255, 255, .62);
+  box-shadow: 0 8px 22px rgba(15, 23, 42, .08);
+}
+.login-logo-frame img {
+  display: block;
+  max-width: 180px;
+  max-height: 58px;
+  object-fit: contain;
+}
+.login-tools {
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  gap: 9px;
+  margin-bottom: 18px;
+}
+.login-label {
+  display: block;
+  color: #1f2937;
+  font-size: 13px;
+  font-weight: 720;
+}
+.login-input {
+  width: 100%;
+  margin: 6px 0 15px;
+  padding: 11px 12px;
+  border: 1px solid rgba(71, 85, 105, .34);
+  border-radius: 9px;
+  background: rgba(255, 255, 255, .74);
+  color: #0f172a;
+  outline: none;
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, .45);
+}
+.login-input:focus {
+  border-color: var(--blue);
+  box-shadow: 0 0 0 3px rgba(47, 155, 216, .2);
+}
+.login-error {
+  margin: 0 0 12px;
+  color: #b91c1c;
+  font-size: 13px;
+  font-weight: 700;
+}
+.login-submit {
+  width: 100%;
+  min-height: 48px;
+  padding: 12px;
+  border: 1px solid rgba(0, 0, 0, .12);
+  border-radius: 9px;
+  font-size: 15px;
+  font-weight: 780;
+  cursor: pointer;
+  box-shadow: 0 10px 22px rgba(15, 23, 42, .14);
+}
+.login-submit:disabled {
+  cursor: wait;
+  opacity: .76;
+}
+[data-theme="dark"] .login-page.has-background::before {
+  background:
+    radial-gradient(circle at 50% 45%, rgba(15, 23, 42, .08), rgba(15, 23, 42, .34)),
+    linear-gradient(180deg, rgba(0, 0, 0, .08), rgba(0, 0, 0, .28));
+}
+[data-theme="dark"] .login-card {
+  border-color: rgba(148, 163, 184, .22);
+  background: rgba(17, 24, 39, .76);
+  color: #f8fafc;
+  box-shadow: 0 20px 54px rgba(0, 0, 0, .42);
+}
+[data-theme="dark"] .login-logo-frame {
+  border-color: rgba(255, 255, 255, .48);
+  background: rgba(255, 255, 255, .78);
+}
+[data-theme="dark"] .login-label {
+  color: #e5e7eb;
+}
+[data-theme="dark"] .login-input {
+  border-color: rgba(226, 232, 240, .72);
+  background: rgba(15, 23, 42, .28);
+  color: #f8fafc;
+}
+[data-theme="dark"] .login-input:focus {
+  border-color: #93c5fd;
+  box-shadow: 0 0 0 3px rgba(147, 197, 253, .24);
+}
+
 .app-shell {
   min-height: 100vh;
   display: grid;
   grid-template-columns: 58px 292px minmax(0, 1fr);
   background: #eef3f4;
 }
+.app-shell.is-collapsed {
+  grid-template-columns: 58px minmax(0, 1fr);
+}
+.app-shell.is-collapsed .sidebar {
+  display: none;
+}
 .icon-rail {
+  position: relative;
   background: #f8fbfb;
   border-right: 1px solid var(--line);
   display: flex;
@@ -38,6 +212,17 @@ label { font-size: 13px; color: #334155; }
   gap: 14px;
   padding: 10px 8px;
 }
+.app-shell.strip-extended .icon-rail {
+  padding-top: 58px;
+}
+.app-shell.strip-extended .icon-rail::before {
+  content: "";
+  position: absolute;
+  top: 0; left: 0; right: -1px;
+  height: 48px;
+  background: var(--blue);
+  z-index: 1;
+}
 .brand-mark {
   width: 34px;
   height: 34px;
@@ -48,7 +233,11 @@ label { font-size: 13px; color: #334155; }
   color: #fff;
   font-weight: 800;
   letter-spacing: 0;
+  border: 0;
+  cursor: pointer;
+  font: inherit;
 }
+.brand-mark:hover { background: var(--brand-dark); }
 .rail-dot {
   width: 34px;
   height: 34px;
@@ -57,20 +246,42 @@ label { font-size: 13px; color: #334155; }
   display: grid;
   place-items: center;
   color: #475467;
-  background: #fff;
+  background: var(--panel);
   font-size: 16px;
+  cursor: pointer;
+  font: inherit;
 }
+.rail-dot:hover { background: #eefaf7; color: var(--brand-dark); border-color: var(--brand); }
 .sidebar {
-  background: #fff;
+  position: relative;
+  background: var(--panel);
   border-right: 1px solid var(--line);
   display: flex;
   flex-direction: column;
   min-width: 0;
 }
+.app-shell.strip-extended .sidebar {
+  padding-top: 48px;
+}
+.app-shell.strip-extended .sidebar::before {
+  content: "";
+  position: absolute;
+  top: 0; left: 0; right: -1px;
+  height: 48px;
+  background: var(--blue);
+  z-index: 1;
+}
 .sidebar-head {
   padding: 18px 18px 14px;
   border-bottom: 1px solid var(--line);
 }
+.sidebar-logo {
+  display: block;
+  max-height: 32px;
+  max-width: 100%;
+  margin-bottom: 6px;
+  object-fit: contain;
+}
 .app-title {
   margin: 0;
   font-size: 17px;
@@ -146,45 +357,113 @@ label { font-size: 13px; color: #334155; }
   padding: 0 18px;
   box-shadow: 0 1px 0 rgba(0,0,0,.08);
 }
+.mobile-top-title {
+  display: none;
+}
 .language-switcher {
+  position: relative;
   display: flex;
   align-items: center;
-  gap: 8px;
 }
-.language-button {
-  width: 42px;
+.language-trigger {
+  width: 38px;
+  height: 32px;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  gap: 4px;
+  border: 1px solid var(--line);
+  border-radius: 8px;
+  background: var(--panel);
+  color: var(--muted);
+  cursor: pointer;
+  padding: 0 6px;
+  transition: background .15s ease, color .15s ease, border-color .15s ease;
+}
+.language-trigger:hover {
+  background: var(--soft);
+  color: var(--ink);
+}
+.topbar .language-trigger {
+  border-color: rgba(255,255,255,.55);
+  background: rgba(255,255,255,.18);
+  color: #fff;
+}
+.topbar .language-trigger:hover {
+  background: rgba(255,255,255,.28);
+}
+.language-flag-mini {
+  width: 16px;
+  height: 11px;
+  object-fit: cover;
+  border-radius: 2px;
+  box-shadow: 0 0 0 1px rgba(15, 23, 42, .15);
+}
+.language-dropdown {
+  position: absolute;
+  top: calc(100% + 6px);
+  right: 0;
+  z-index: 40;
+  min-width: 190px;
+  background: var(--panel);
+  border: 1px solid var(--line);
+  border-radius: 10px;
+  box-shadow: var(--shadow);
+  padding: 6px;
+  display: flex;
+  flex-direction: column;
+  gap: 2px;
+}
+.language-option {
+  display: flex;
+  align-items: center;
+  gap: 10px;
+  border: none;
+  background: transparent;
+  color: var(--ink);
+  padding: 8px 10px;
+  border-radius: 8px;
+  cursor: pointer;
+  font-size: 13px;
+  text-align: left;
+  width: 100%;
+}
+.language-option:hover {
+  background: var(--soft);
+}
+.language-option.is-active {
+  background: var(--soft);
+  font-weight: 600;
+}
+.language-option .language-flag {
+  width: 22px;
+  height: 16px;
+}
+.theme-toggle {
+  width: 32px;
   height: 32px;
   display: grid;
   place-items: center;
   border: 1px solid var(--line);
   border-radius: 8px;
-  background: #fff;
-  padding: 3px;
+  background: var(--panel);
+  color: var(--muted);
   cursor: pointer;
-  transition: background .15s ease, border-color .15s ease, transform .15s ease;
-}
-.language-button:hover {
-  background: #f8fbfb;
+  transition: background .15s ease, color .15s ease, border-color .15s ease;
 }
-.language-button.is-active {
-  background: #fff;
-  border-color: var(--brand);
-  box-shadow: inset 0 -3px 0 var(--brand);
+.theme-toggle:hover {
+  background: var(--soft);
+  color: var(--ink);
 }
-.topbar .language-button {
+.topbar .theme-toggle {
   border-color: rgba(255,255,255,.55);
   background: rgba(255,255,255,.18);
+  color: #fff;
 }
-.topbar .language-button:hover {
+.topbar .theme-toggle:hover {
   background: rgba(255,255,255,.28);
 }
-.topbar .language-button.is-active {
-  background: #fff;
-  border-color: #fff;
-}
 .language-flag {
-  width: 100%;
-  height: 100%;
   display: block;
   border-radius: 5px;
   object-fit: cover;
@@ -229,7 +508,7 @@ label { font-size: 13px; color: #334155; }
 .btn-primary:hover { background: var(--brand-dark); }
 .btn-ghost {
   border: 1px solid var(--line);
-  background: #fff;
+  background: var(--panel);
   color: #344054;
   padding: 8px 12px;
 }
@@ -244,7 +523,7 @@ label { font-size: 13px; color: #334155; }
 }
 .btn-mini {
   border: 1px solid var(--line);
-  background: #fff;
+  background: var(--panel);
   color: #344054;
   padding: 5px 8px;
   font-size: 12px;
@@ -259,82 +538,480 @@ label { font-size: 13px; color: #334155; }
   border-color: #f2b8b5;
   color: #b42318;
 }
-.btn-mini.action-primary {
-  border-color: var(--brand);
-  background: #e0f4f0;
-  color: var(--brand-dark);
+.btn-icon {
+  border: 1px solid var(--line);
+  background: var(--panel);
+  color: #94a3b8;
+  border-radius: 999px;
+  width: 24px;
+  height: 24px;
+  padding: 0;
+  margin: 0 4px 4px 0;
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  cursor: pointer;
+}
+.btn-icon:hover {
+  color: #344054;
+  border-color: #344054;
+}
+.btn-icon.toll {
+  border-color: #f4d58d;
+  background: #fdf6e3;
+  color: #b8860b;
+}
+.btn-icon.toll:hover {
+  background: #fbe9b8;
+  border-color: #d4a017;
+  color: #8a6200;
+}
+.toll-list {
+  list-style: none;
+  margin: 0 0 10px;
+  padding: 0;
+  display: grid;
+  gap: 6px;
+}
+.toll-list li {
+  display: flex;
+  align-items: center;
+  gap: 8px;
+  padding: 6px 10px;
+  border: 1px solid #f4d58d;
+  background: #fdf6e3;
+  border-radius: 8px;
+  font-size: 13px;
+}
+.toll-list li.ida {
+  border-color: #a8d8f0;
+  background: #eaf6fd;
+}
+.toll-list li.volta {
+  border-color: #f6c89f;
+  background: #fef1e6;
+}
+.toll-list .toll-dir {
+  font-weight: 700;
+  color: #8a6200;
+  flex: 1;
+}
+.toll-list li.ida .toll-dir {
+  color: #0e74a8;
+}
+.toll-list li.volta .toll-dir {
+  color: #c2620a;
+}
+.toll-list .toll-amount {
+  font-weight: 700;
+  color: #344054;
+}
+.toll-list .btn-icon {
+  color: #97650a;
+  border-color: #f4d58d;
+  background: var(--panel);
+}
+.toll-list li.ida .btn-icon {
+  color: #0e74a8;
+  border-color: #a8d8f0;
+}
+.toll-list li.volta .btn-icon {
+  color: #c2620a;
+  border-color: #f6c89f;
+}
+.toll-list .btn-icon:hover {
+  background: #fee2e2;
+  color: #b42318;
+  border-color: #fca5a5;
+}
+.btn-mini.action-primary {
+  border-color: var(--brand);
+  background: #e0f4f0;
+  color: var(--brand-dark);
+}
+.danger-bg {
+  background: var(--danger);
+}
+.danger-bg:hover {
+  background: #b42318;
+}
+.card-panel {
+  background: var(--panel);
+  border: 1px solid var(--line);
+  border-radius: 8px;
+  box-shadow: var(--shadow);
+}
+.form-panel { padding: 14px; margin-bottom: 14px; }
+.form-grid {
+  display: grid;
+  grid-template-columns: repeat(auto-fit, minmax(190px, 1fr));
+  gap: 10px;
+}
+.field {
+  display: flex;
+  flex-direction: column;
+  gap: 4px;
+  font-size: 13px;
+  color: #475467;
+}
+.input {
+  width: 100%;
+  border: 1px solid #cbd5df;
+  border-radius: 7px;
+  padding: 8px 9px;
+  background: var(--panel);
+  min-height: 36px;
+}
+.data-table {
+  width: 100%;
+  border-collapse: collapse;
+  background: var(--panel);
+  border: 1px solid var(--line);
+  border-radius: 8px;
+  overflow: hidden;
+}
+.data-table th {
+  padding: 9px 10px;
+  color: #475467;
+  font-size: 12px;
+  background: #f4f7f8;
+  text-align: left;
+  white-space: nowrap;
+}
+.data-table td {
+  padding: 9px 10px;
+  border-top: 1px solid #edf1f4;
+  font-size: 13px;
+  vertical-align: top;
+}
+.table-scroll { overflow: auto; border-radius: 8px; }
+
+.stats-grid {
+  display: grid;
+  grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
+  gap: 12px;
+}
+.stat-card {
+  background: var(--panel);
+  border: 1px solid var(--line);
+  border-radius: 8px;
+  padding: 14px;
+  box-shadow: var(--shadow);
+}
+.stat-label { font-size: 12px; color: var(--muted); }
+.stat-value { font-size: 30px; font-weight: 850; margin-top: 4px; }
+.dashboard-header {
+  align-items: center;
+}
+.dashboard-controls {
+  display: flex;
+  flex-wrap: wrap;
+  justify-content: flex-end;
+  gap: 8px;
+}
+.dashboard-filter button {
+  min-width: 72px;
+}
+.week-picker {
+  display: flex;
+  align-items: center;
+  gap: 7px;
+  min-height: 34px;
+  padding: 3px 8px;
+  border: 1px solid var(--line);
+  border-radius: 8px;
+  background: var(--panel);
+  color: #475467;
+  font-size: 12px;
+  font-weight: 800;
+}
+.week-picker input {
+  border: 0;
+  color: var(--ink);
+  background: transparent;
+  font-size: 12px;
+  font-weight: 800;
+  outline: none;
+}
+.dashboard-grid {
+  display: grid;
+  grid-template-columns: minmax(0, 1.35fr) minmax(320px, .65fr);
+  gap: 12px;
+}
+.dashboard-kpis {
+  grid-column: 1 / -1;
+  display: grid;
+  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
+  gap: 12px;
+}
+.dashboard-ops-grid {
+  grid-column: 1 / -1;
+  display: grid;
+  grid-template-columns: repeat(5, minmax(120px, 1fr));
+  gap: 8px;
+}
+.mini-metric {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 8px;
+  min-height: 46px;
+  padding: 9px 12px;
+  border: 1px solid var(--line);
+  border-radius: 8px;
+  background: var(--panel);
+  box-shadow: 0 2px 8px rgba(16, 24, 40, .04);
+}
+.mini-metric span {
+  color: #475467;
+  font-size: 12px;
+  font-weight: 750;
+}
+.mini-metric strong {
+  color: var(--brand-dark);
+  font-size: 19px;
+}
+.dashboard-kpi {
+  position: relative;
+  overflow: hidden;
+}
+.dashboard-kpi::before {
+  content: "";
+  position: absolute;
+  inset: 0 auto 0 0;
+  width: 4px;
+  background: var(--blue);
+}
+.dashboard-kpi.tone-success::before { background: #16a34a; }
+.dashboard-kpi.tone-danger::before { background: #d64545; }
+.dashboard-kpi.tone-warning::before { background: #f59e0b; }
+.kpi-detail {
+  margin-top: 4px;
+  color: var(--muted);
+  font-size: 12px;
+}
+.dashboard-chart-card {
+  padding: 14px;
+  min-width: 0;
+  display: grid;
+  grid-template-rows: auto minmax(0, 1fr) auto;
+  align-content: start;
+}
+.dashboard-chart-card.chart-wide {
+  min-height: 300px;
+}
+.chart-head {
+  display: flex;
+  justify-content: space-between;
+  gap: 12px;
+  margin-bottom: 12px;
+}
+.chart-head h3 {
+  margin: 0;
+  font-size: 16px;
+}
+.chart-head p {
+  margin: 3px 0 0;
+  color: var(--muted);
+  font-size: 12px;
+}
+.combo-chart {
+  display: flex;
+  gap: 8px;
+  align-items: end;
+  height: 205px;
+  min-height: 205px;
+  overflow-x: auto;
+  overflow-y: hidden;
+  padding: 20px 4px 0;
+  border-bottom: 1px solid var(--line);
+  scrollbar-width: thin;
+  scrollbar-color: #b8c6d2 transparent;
+}
+.combo-chart::-webkit-scrollbar { height: 8px; }
+.combo-chart::-webkit-scrollbar-thumb { background: #b8c6d2; border-radius: 999px; }
+.combo-chart::-webkit-scrollbar-track { background: transparent; }
+.combo-bar {
+  flex: 0 0 58px;
+  display: grid;
+  grid-template-rows: 1fr auto;
+  gap: 7px;
+  align-items: end;
+}
+.combo-stack {
+  height: 165px;
+  display: flex;
+  align-items: end;
+  justify-content: center;
+  gap: 4px;
+}
+.combo-stack span {
+  position: relative;
+  width: 12px;
+  min-height: 4px;
+  border-radius: 5px 5px 0 0;
+}
+.combo-stack em {
+  position: absolute;
+  left: 50%;
+  top: -17px;
+  transform: translateX(-50%);
+  color: #667085;
+  font-size: 9px;
+  font-style: normal;
+  font-weight: 800;
+  line-height: 1;
+  white-space: nowrap;
+}
+.bar-routes {
+  background: #2f9bd8;
+}
+.bar-deliveries {
+  background: var(--brand);
+}
+.bar-dock {
+  background: #2f9bd8;
+}
+.bar-loading {
+  background: var(--brand);
+}
+.bar-release {
+  background: #7c3aed;
+}
+.bar-stops {
+  background: #f59e0b;
+}
+.bar-weight {
+  background: #2563eb;
+}
+.bar-pallets {
+  background: #14b8a6;
+}
+.bar-toll-out {
+  background: #0ea5e9;
+}
+.bar-toll-return {
+  background: #6366f1;
+}
+.bar-toll-total {
+  background: #10b981;
+}
+.time-chart .combo-stack span {
+  width: 8px;
+}
+.time-chart .combo-bar {
+  flex-basis: 64px;
+}
+.load-chart .combo-bar {
+  flex-basis: 62px;
+}
+.toll-chart .combo-stack span {
+  width: 9px;
+}
+.toll-chart .combo-bar {
+  flex-basis: 64px;
+}
+.combo-bar small {
+  color: var(--muted);
+  font-size: 10px;
+  text-align: center;
+  white-space: nowrap;
+  transform: rotate(-28deg);
+  height: 24px;
+  line-height: 1;
+}
+.chart-legend {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 12px;
+  margin-top: 8px;
+  min-height: 18px;
+  color: #475467;
+  font-size: 12px;
+  font-weight: 750;
+}
+.chart-legend span {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+}
+.chart-legend i {
+  width: 10px;
+  height: 10px;
+  border-radius: 3px;
+  display: inline-block;
+}
+.stack-chart {
+  display: grid;
+  gap: 9px;
+}
+.stack-row {
+  display: grid;
+  grid-template-columns: 62px minmax(0, 1fr) 30px;
+  align-items: center;
+  gap: 8px;
+}
+.stack-row span {
+  color: #475467;
+  font-size: 12px;
+}
+.stack-row strong {
+  font-size: 12px;
+  text-align: right;
+}
+.stack-track {
+  height: 12px;
+  display: flex;
+  overflow: hidden;
+  border-radius: 999px;
+  background: #edf2f7;
+}
+.stack-track i {
+  display: block;
+  min-width: 0;
 }
-.danger-bg {
-  background: var(--danger);
+.stack-success {
+  background: #16a34a;
 }
-.danger-bg:hover {
-  background: #b42318;
+.stack-failure {
+  background: #d64545;
 }
-.card-panel {
-  background: var(--panel);
-  border: 1px solid var(--line);
-  border-radius: 8px;
-  box-shadow: var(--shadow);
+.status-list {
+  display: grid;
+  gap: 12px;
 }
-.form-panel { padding: 14px; margin-bottom: 14px; }
-.form-grid {
+.status-row {
   display: grid;
-  grid-template-columns: repeat(auto-fit, minmax(190px, 1fr));
-  gap: 10px;
+  gap: 6px;
 }
-.field {
+.status-row > div:first-child {
   display: flex;
-  flex-direction: column;
-  gap: 4px;
-  font-size: 13px;
+  justify-content: space-between;
+  gap: 8px;
   color: #475467;
+  font-size: 13px;
 }
-.input {
-  width: 100%;
-  border: 1px solid #cbd5df;
-  border-radius: 7px;
-  padding: 8px 9px;
-  background: #fff;
-  min-height: 36px;
+.status-row strong {
+  color: var(--ink);
 }
-.data-table {
-  width: 100%;
-  border-collapse: collapse;
-  background: #fff;
-  border: 1px solid var(--line);
-  border-radius: 8px;
+.status-track {
+  height: 10px;
   overflow: hidden;
+  border-radius: 999px;
+  background: #edf2f7;
 }
-.data-table th {
-  padding: 9px 10px;
-  color: #475467;
-  font-size: 12px;
-  background: #f4f7f8;
-  text-align: left;
-  white-space: nowrap;
-}
-.data-table td {
-  padding: 9px 10px;
-  border-top: 1px solid #edf1f4;
-  font-size: 13px;
-  vertical-align: top;
+.status-track i {
+  display: block;
+  height: 100%;
+  border-radius: inherit;
+  background: var(--brand);
 }
-.table-scroll { overflow: auto; border-radius: 8px; }
-
-.stats-grid {
-  display: grid;
-  grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
-  gap: 12px;
+.failure-row .status-track i {
+  background: #d64545;
 }
-.stat-card {
-  background: #fff;
-  border: 1px solid var(--line);
-  border-radius: 8px;
-  padding: 14px;
-  box-shadow: var(--shadow);
+.delivery-status-row .status-track i {
+  background: #2f9bd8;
 }
-.stat-label { font-size: 12px; color: var(--muted); }
-.stat-value { font-size: 30px; font-weight: 850; margin-top: 4px; }
 
 .planning-board {
   display: grid;
@@ -356,6 +1033,10 @@ label { font-size: 13px; color: #334155; }
   justify-content: space-between;
   gap: 10px;
 }
+.route-list-close,
+.route-list-toggle {
+  display: none;
+}
 .route-filter {
   display: flex;
   gap: 4px;
@@ -375,7 +1056,7 @@ label { font-size: 13px; color: #334155; }
   padding: 5px 7px;
 }
 .route-filter button.is-active {
-  background: #fff;
+  background: var(--panel);
   color: var(--brand-dark);
   box-shadow: 0 1px 4px rgba(16,24,40,.08);
 }
@@ -389,7 +1070,7 @@ label { font-size: 13px; color: #334155; }
   border: 1px solid #e2e8f0;
   border-left: 5px solid var(--brand);
   border-radius: 8px;
-  background: #fff;
+  background: var(--panel);
   padding: 10px;
   box-shadow: 0 2px 8px rgba(16, 24, 40, .04);
 }
@@ -446,7 +1127,7 @@ label { font-size: 13px; color: #334155; }
   top: 10px;
   left: 10px;
   display: flex;
-  background: #fff;
+  background: var(--panel);
   border: 1px solid var(--line);
   border-radius: 8px;
   overflow: hidden;
@@ -492,6 +1173,18 @@ label { font-size: 13px; color: #334155; }
   height: 34px;
   box-shadow: 0 0 0 5px rgba(18,163,134,.22), 0 8px 18px rgba(0,0,0,.28);
 }
+.leaflet-route-pin.status-entregue {
+  border-color: #f0fdf4;
+  box-shadow: 0 0 0 4px rgba(22,163,74,.22), 0 7px 16px rgba(0,0,0,.28);
+}
+.leaflet-route-pin.status-carregado {
+  border-color: #eff6ff;
+  box-shadow: 0 0 0 4px rgba(37,99,235,.22), 0 7px 16px rgba(0,0,0,.28);
+}
+.leaflet-route-pin.status-falha {
+  border-color: #fef2f2;
+  box-shadow: 0 0 0 4px rgba(239,68,68,.22), 0 7px 16px rgba(0,0,0,.28);
+}
 .leaflet-route-pin span {
   transform: rotate(45deg);
   font-size: 11px;
@@ -598,7 +1291,7 @@ label { font-size: 13px; color: #334155; }
   border: 1px solid #e2e8f0;
   border-radius: 8px;
   padding: 9px;
-  background: #fff;
+  background: var(--panel);
 }
 .delivery-seq {
   width: 26px;
@@ -652,19 +1345,24 @@ label { font-size: 13px; color: #334155; }
 .modal-backdrop {
   position: fixed;
   inset: 0;
-  z-index: 50;
+  z-index: 5000;
   display: grid;
   place-items: center;
   padding: 16px;
   background: rgba(16,24,40,.48);
 }
 .modal-card {
-  width: min(420px, 100%);
-  background: #fff;
+  width: min(680px, 100%);
+  max-height: min(760px, calc(100vh - 32px));
+  overflow: auto;
+  background: var(--panel);
   border-radius: 8px;
   box-shadow: 0 18px 50px rgba(0,0,0,.22);
   padding: 18px;
 }
+.modal-card-compact {
+  width: min(420px, 100%);
+}
 .modal-card h3 {
   margin: 0 0 4px;
 }
@@ -673,39 +1371,326 @@ label { font-size: 13px; color: #334155; }
   color: var(--muted);
   font-size: 14px;
 }
+.modal-card .modal-error {
+  color: #b91c1c;
+  background: #fef2f2;
+  border: 1px solid #fecaca;
+  border-radius: 8px;
+  padding: 8px;
+}
+.help-faq {
+  display: grid;
+  gap: 10px;
+  margin-top: 12px;
+}
+.help-faq-item {
+  border: 1px solid var(--line);
+  border-radius: 8px;
+  padding: 10px 12px;
+  background: #f8fbfb;
+}
+.help-faq-item h4 {
+  margin: 0 0 5px;
+  font-size: 14px;
+}
+.help-faq-item p {
+  margin: 0;
+  line-height: 1.45;
+}
+.help-contact {
+  margin: 14px 0 0 !important;
+  font-weight: 750;
+  color: #344054 !important;
+}
 .modal-actions {
   display: flex;
   gap: 8px;
   margin-top: 12px;
 }
+.section-head {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 12px;
+  margin-bottom: 12px;
+}
+.section-head h3 {
+  margin: 0 0 4px;
+}
+.section-head p {
+  margin: 0;
+  color: var(--muted);
+  font-size: 13px;
+}
+.section-head strong {
+  color: #059669;
+  font-size: 20px;
+  white-space: nowrap;
+}
+.report-tabs {
+  display: inline-flex;
+  gap: 4px;
+  padding: 4px;
+  margin: 0 0 14px;
+  border: 1px solid var(--line);
+  border-radius: 8px;
+  background: var(--panel);
+}
+.report-tabs button {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+  min-height: 34px;
+  padding: 6px 10px;
+  border: 1px solid transparent;
+  border-radius: 6px;
+  background: transparent;
+  color: var(--muted);
+  font-weight: 750;
+  cursor: pointer;
+}
+.report-tabs button.active {
+  border-color: var(--line);
+  background: #f8fbfb;
+  color: var(--text);
+}
+.warehouse-proof-list {
+  display: grid;
+  gap: 10px;
+  margin-top: 12px;
+}
+.warehouse-proof-item {
+  padding: 10px;
+  border: 1px solid var(--line);
+  border-radius: 8px;
+  background: #f8fbfb;
+}
+.warehouse-proof-item span {
+  display: grid;
+  gap: 2px;
+  color: var(--text);
+  font-weight: 750;
+}
+.warehouse-proof-item small {
+  color: var(--muted);
+  font-weight: 500;
+}
 
 .manifest-layout {
   display: grid;
-  grid-template-columns: 320px minmax(0, 1fr);
+  grid-template-columns: minmax(240px, 320px) minmax(0, 1fr);
   gap: 16px;
   align-items: start;
+  min-width: 0;
+  overflow: hidden;
 }
 .manifest-detail-grid {
   display: grid;
   grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
   gap: 16px;
+  min-width: 0;
+}
+.manifest-history-panel,
+.manifest-main-panel,
+.manifest-detail-grid > div {
+  min-width: 0;
+}
+.manifest-history-panel {
+  width: 100%;
+  max-width: 100%;
+  overflow: hidden;
+  position: relative;
+  z-index: 1;
+}
+.manifest-history-list {
+  display: grid;
+  gap: 8px;
+  max-height: calc(100vh - 260px);
+  min-height: 220px;
+  overflow-y: auto;
+  overflow-x: hidden;
+  padding-right: 6px;
+  margin-right: -2px;
+}
+.manifest-history-item {
+  width: 100%;
+  max-width: 100%;
+  min-width: 0;
+  overflow: hidden;
+  outline: none;
+}
+.manifest-history-item:focus-visible {
+  box-shadow: inset 3px 0 0 #0a58ca, 0 0 0 2px rgba(10, 88, 202, .14) !important;
+}
+.manifest-history-item span,
+.manifest-history-item strong {
+  min-width: 0;
+  max-width: 100%;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+.manifest-main-panel {
+  overflow: hidden;
+  position: relative;
+  z-index: 0;
+}
+.manifest-file-frame {
+  width: 100%;
+  height: min(620px, calc(100vh - 280px));
+  min-height: 420px;
+  border: 1px solid #cbd5e1;
+  border-radius: 8px;
+  display: block;
+}
+.manifest-actions {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 8px;
+  margin-top: 12px;
+}
+.expense-toolbar {
+  display: flex;
+  align-items: flex-end;
+  gap: 12px;
+  padding: 12px 16px;
+  margin-bottom: 14px;
+}
+.expense-actions {
+  display: flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 8px;
+  margin-left: auto;
+}
+.btn-icon-label {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+  padding: 6px 12px;
+  border: 1px solid var(--line);
+  border-radius: 8px;
+  background: var(--panel);
+  color: #475569;
+  font-size: 13px;
+  cursor: pointer;
+  transition: background 0.15s, border-color 0.15s;
+}
+.btn-icon-label:hover {
+  background: #f1f5f9;
+  border-color: #94a3b8;
+  color: #1e293b;
+}
+.btn-icon-label:disabled {
+  opacity: .6;
+  cursor: wait;
+}
+.report-updated {
+  margin: -4px 0 12px;
+  color: #047857;
+  font-size: 13px;
+  font-weight: 650;
+}
+.btn-add {
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+}
+.btn-add-symbol {
+  display: inline-grid;
+  place-items: center;
+  width: 18px;
+  height: 18px;
+  border-radius: 999px;
+  background: rgba(255,255,255,.2);
+  font-size: 18px;
+  line-height: 1;
+  font-weight: 850;
+}
+.upload-pill {
+  position: relative;
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  min-height: 38px;
+  width: 100%;
+  padding: 7px 12px 7px 8px;
+  border: 1px solid #cbd5e1;
+  border-radius: 999px;
+  background: #e5e7eb;
+  color: #334155;
+  font-weight: 800;
+  cursor: pointer;
+  overflow: hidden;
+}
+.upload-pill input {
+  position: absolute;
+  inset: 0;
+  opacity: 0;
+  cursor: pointer;
+}
+.upload-pill-icon {
+  display: inline-grid;
+  place-items: center;
+  width: 26px;
+  height: 26px;
+  border-radius: 999px;
+  background: var(--panel);
+  color: #64748b;
+  flex: 0 0 auto;
+}
+.upload-pill span:last-child {
+  min-width: 0;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
 }
 @media (max-width: 960px) {
   .manifest-layout { grid-template-columns: 1fr; }
   .manifest-detail-grid { grid-template-columns: 1fr; }
+  .manifest-history-list { max-height: 320px; }
+  .manifest-file-frame { height: 520px; }
+  .expense-toolbar { flex-wrap: wrap; }
+  .expense-actions { margin-left: 0; justify-content: flex-start; }
+  .btn-icon-label { flex: 1 1 120px; justify-content: center; }
 }
 
 @media (max-width: 1020px) {
-  .app-shell { grid-template-columns: 54px minmax(0, 1fr); }
+  .app-shell {
+    min-height: 100dvh;
+    grid-template-columns: minmax(0, 1fr);
+    grid-template-rows: auto auto minmax(0, 1fr);
+  }
   .sidebar {
     grid-column: 1 / -1;
     grid-row: 1;
     border-right: 0;
     border-bottom: 1px solid var(--line);
   }
+  .app-shell.strip-extended .sidebar {
+    padding-top: 0;
+  }
+  .app-shell.strip-extended .sidebar::before {
+    position: static;
+    display: block;
+    height: 48px;
+  }
   .icon-rail { display: none; }
-  .content-shell { grid-column: 1 / -1; }
-  .sidebar-head { padding: 12px 14px; }
+  .content-shell {
+    grid-column: 1 / -1;
+    grid-row: 2 / 4;
+    min-height: 0;
+  }
+  .sidebar-head {
+    display: grid;
+    justify-items: center;
+    text-align: center;
+    padding: 14px 14px 12px;
+  }
+  .sidebar-logo {
+    margin: 0 auto 7px;
+    max-height: 34px;
+  }
   .nav-groups {
     display: flex;
     overflow-x: auto;
@@ -718,19 +1703,140 @@ label { font-size: 13px; color: #334155; }
   .sidebar-user { display: none; }
   .topbar { height: 44px; }
   .page-main { padding: 12px; }
+  .dashboard-header { align-items: stretch; }
+  .dashboard-controls { justify-content: flex-start; }
+  .dashboard-grid { grid-template-columns: 1fr; }
+  .dashboard-kpis { grid-template-columns: repeat(2, minmax(0, 1fr)); }
+  .dashboard-ops-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }
   .planning-board { grid-template-columns: 1fr; height: auto; min-height: 0; }
   .route-list-panel { max-height: 360px; }
   .planning-main { grid-template-rows: 300px auto auto; }
 }
 
 @media (max-width: 640px) {
+  .login-page {
+    align-items: center;
+    padding: max(16px, env(safe-area-inset-top)) 14px max(16px, env(safe-area-inset-bottom));
+    background-position: center center;
+  }
+  .login-page.has-background {
+    background-size: contain;
+    background-color: #063f73;
+  }
+  .login-card {
+    width: min(430px, calc(100vw - 28px));
+    padding: 26px 22px;
+    border-radius: 14px;
+  }
+  .login-brand {
+    min-height: 70px;
+  }
+  .login-logo-frame img {
+    max-width: 160px;
+    max-height: 54px;
+  }
   .page-header { flex-direction: column; align-items: stretch; }
   .page-header .btn-primary { width: 100%; }
-  .topbar { justify-content: center; padding: 0 10px; }
+  .page-header .btn-add {
+    width: auto;
+    min-width: 42px;
+    min-height: 42px;
+    align-self: flex-end;
+    justify-content: center;
+    border-radius: 999px;
+    padding: 0 13px;
+  }
+  .page-header .btn-add span:not(.btn-add-symbol) { display: none; }
+  .topbar {
+    position: relative;
+    justify-content: flex-end;
+    padding: 0 10px;
+  }
+  .mobile-top-title {
+    display: block;
+    position: absolute;
+    left: 50%;
+    transform: translateX(-50%);
+    max-width: calc(100% - 108px);
+    overflow: hidden;
+    text-overflow: ellipsis;
+    white-space: nowrap;
+    color: #fff;
+    font-size: 14px;
+    font-weight: 850;
+    text-align: center;
+    pointer-events: none;
+  }
   .page-main { padding: 10px; }
   .form-grid { grid-template-columns: 1fr; }
   .stats-grid { grid-template-columns: 1fr 1fr; }
-  .planning-main { grid-template-rows: 240px auto auto; }
+  .dashboard-controls { display: grid; grid-template-columns: 1fr; }
+  .dashboard-filter { width: 100%; }
+  .dashboard-filter button { flex: 1; min-width: 0; }
+  .week-picker { justify-content: space-between; width: 100%; }
+  .dashboard-kpis { grid-template-columns: 1fr; }
+  .dashboard-ops-grid { grid-template-columns: 1fr; }
+  .dashboard-chart-card { padding: 12px; }
+  .combo-chart { gap: 4px; height: 180px; min-height: 180px; padding-top: 18px; }
+  .combo-bar { flex-basis: 50px; }
+  .combo-stack { height: 140px; gap: 3px; }
+  .combo-stack span { width: 9px; }
+  .combo-stack em { font-size: 8px; top: -15px; }
+  .time-chart .combo-stack span { width: 6px; }
+  .time-chart .combo-bar { flex-basis: 58px; }
+  .combo-bar small { font-size: 9px; transform: rotate(-35deg); }
+  .stack-row { grid-template-columns: 52px minmax(0, 1fr) 28px; }
+  .planning-board.has-selected-route:not(.is-route-list-expanded) .route-list-panel { display: none; }
+  .planning-board.is-route-list-expanded .route-list-panel {
+    display: flex;
+    max-height: 58vh;
+    box-shadow: 0 14px 28px rgba(16, 24, 40, .16);
+  }
+  .planning-board.is-route-list-expanded .route-list {
+    max-height: calc(58vh - 60px);
+  }
+  .route-list-head {
+    align-items: flex-start;
+    position: relative;
+    padding-right: 42px;
+  }
+  .route-list-close {
+    display: grid;
+    position: absolute;
+    top: 10px;
+    right: 10px;
+    width: 28px;
+    height: 28px;
+    place-items: center;
+    border: 1px solid var(--line);
+    border-radius: 999px;
+    background: var(--panel);
+    color: #344054;
+    font-size: 20px;
+    line-height: 1;
+    cursor: pointer;
+  }
+  .route-list-toggle {
+    display: inline-flex;
+    position: absolute;
+    top: 10px;
+    right: 10px;
+    z-index: 510;
+    align-items: center;
+    gap: 6px;
+    min-height: 34px;
+    border: 1px solid rgba(15, 23, 42, .14);
+    border-radius: 8px;
+    background: rgba(255,255,255,.95);
+    color: #0f172a;
+    padding: 7px 10px;
+    font-size: 12px;
+    font-weight: 850;
+    box-shadow: 0 8px 18px rgba(16, 24, 40, .16);
+    cursor: pointer;
+  }
+  .planning-main { grid-template-rows: 220px auto; }
+  .planning-main > .card-panel:last-child { display: none; }
   .ops-tabs { grid-template-columns: 1fr 1fr; }
   .route-list-panel { max-height: none; }
   .route-list-head { align-items: stretch; flex-direction: column; }
@@ -742,6 +1848,17 @@ label { font-size: 13px; color: #334155; }
   .delivery-head .btn-primary { width: 100%; }
   .route-toolbar,
   .manifest-inline { justify-content: stretch; }
+  .expense-actions .btn-icon-label {
+    flex: 0 0 auto;
+    width: 38px;
+    height: 38px;
+    padding: 0;
+  }
+  .expense-actions .btn-icon-label span { display: none; }
+  .upload-pill {
+    min-height: 42px;
+    border-radius: 999px;
+  }
   .manifest-inline input,
   .manifest-inline button,
   .route-toolbar > button { width: 100%; max-width: none; }
